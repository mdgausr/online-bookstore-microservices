# Use npm ci/install inside project to generate a package-lock.json that matches your environment.
# You can add a generated package-lock.json to the repo after running a clean install locally.
# Recommended Node version: 18.x (use nvm or nvm-windows)
