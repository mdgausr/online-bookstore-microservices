import { Component } from '@angular/core';

@Component({
  selector: 'app-account',
  template: `
    <h2>Account</h2>
    <p>Register and login are available via the Identity API. Use the API endpoints directly or add forms here.</p>
  `
})
export class AccountComponent { }
