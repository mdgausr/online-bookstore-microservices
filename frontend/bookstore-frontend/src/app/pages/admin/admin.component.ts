import { Component } from '@angular/core';

@Component({
  selector: 'app-admin',
  template: `
    <h2>Admin</h2>
    <p>Admin panel: manage products (add/edit/delete) — this is a minimal UI. Use Swagger for admin endpoints.</p>
  `
})
export class AdminComponent { }
